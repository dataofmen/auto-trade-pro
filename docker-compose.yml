version: '3.8'

services:
  app:
    build: .
    container_name: auto-trade-pro
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - trade-network

networks:
  trade-network:
    driver: bridge
